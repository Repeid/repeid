apply plugin: 'war'

dependencies {
	// Project Dependencies
	compile project(':repeid-manager:repeid-manager-api:repeid-manager-api-war')

    // Third Party Dependencies
    compile ( libraries.slf4j_api )
    
    provided ( libraries.cdi_api )
    provided ( libraries.jboss_servlet )
    provided ( libraries.keycloak_core )
}